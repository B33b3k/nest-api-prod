# Application
NODE_ENV=development
PORT=3000
API_PREFIX=api
API_VERSION=v1

# Database (Prisma with PostgreSQL)
DATABASE_URL="postgresql://username:password@localhost:5432/nest_pro_api?schema=public"

# Rate Limiting (@nestjs/throttler)
THROTTLE_TTL=60
THROTTLE_LIMIT=100

# Logging Configuration (Winston)
LOG_LEVEL=info # Options: error, warn, info, debug
LOG_FORMAT=combined # Options: combined, tiny, dev
LOG_DIR=logs
ERROR_LOG=error-%DATE%.log
COMBINED_LOG=combined-%DATE%.log
MAX_FILES=30d # How long to keep logs
MAX_SIZE=20m # Max size of each log file

# Security
CORS_ORIGIN=* # In production, change to your frontend domain
CORS_CREDENTIALS=true

# API Documentation
SWAGGER_PATH=docs
SWAGGER_TITLE=NestJS Pro API
SWAGGER_DESCRIPTION=Enterprise-grade NestJS API with Prisma, logging, and rate limiting
SWAGGER_VERSION=1.0

# Optional: If implementing authentication later
# JWT_SECRET=your-super-secret-key
# JWT_EXPIRATION=1h
# REFRESH_TOKEN_EXPIRATION=7d

# Optional: If implementing mail service
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASS=your-password
# MAIL_FROM=noreply@yourapp.com